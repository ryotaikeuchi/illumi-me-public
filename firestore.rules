rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ユーザーは自身のドキュメントのみ読み書き できる
    match /users/{userId} {
      allow read, write: if request.auth.uid == userId;
    }
  }
}